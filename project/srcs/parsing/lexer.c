#include "../../incs/minishell.h"

bool	lexer(char *line)
{
	void(line);
	//int		i;
	//t_token	*token_temp;
//
	//i = -1;
	//token_temp = (t_token *)safe_malloc(sizeof(t_token *));
	//while (line[++i] != '\0')
	//{
	//	while (isspace(line[i]))
	//	{
	//		ft_straddchr(token_temp->token_content, line[i++]);
	//		if (!isspace(line[i]))
	//		{
	//			token_temp->token_type = SPACE;
	//			ft_lstadd_back(&(g_envp->token_list), ft_lstnew(token_temp));
	//			free(token_temp->token_content);
	//		}
	//	}
	//}
	//free(token_temp);
	//return (true);
}